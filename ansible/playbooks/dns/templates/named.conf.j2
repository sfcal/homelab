acl trusted {
  localhost;
  localnets;
  10.2.0.0/16;
  10.1.0.0/16;
};

options {
  directory "/var/cache/bind";
  
  // Enable recursion
  recursion yes;
  
  // Forward to Cloudflare DNS for external resolution
  forwarders {
    1.1.1.1;
    1.0.0.1;
  };
  
  // Enable query logging for debugging
  querylog yes;
  
  // Allow queries from trusted networks
  allow-query { trusted; };
  
  // Allow recursive queries from trusted networks
  allow-recursion { trusted; };
  
  // Prevent zone transfers
  allow-transfer { none; };
  
  // Enable DNSSEC validation
  dnssec-validation auto;
  
  // Listen on all interfaces
  listen-on { any; };
  listen-on-v6 { any; };
};

// Fix for plex forwarding
zone "plex.sfc.al" IN {
  type forward;
  forward only;
  forwarders { 1.1.1.1; 1.0.0.1; };
};

// Split horizon zone for public domain
zone "sfc.al" IN {
  type master;
  file "/etc/bind/sfc-al.zone";
};

// Reverse zone for 10.1.30.0/24
zone "30.1.10.in-addr.arpa" IN {
  type master;
  file "/etc/bind/30.1.10.in-addr.arpa.zone";
};
