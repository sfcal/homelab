---
- name: Deploy Tailscale
  hosts: infra_reverse-proxy
  become: true

  pre_tasks:
    - name: Include common prerequisites.
      ansible.builtin.include_role:
        name: common

  roles:
    - tailscale
