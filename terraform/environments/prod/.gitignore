# Environment-specific variables
terraform.tfvars

# Backend configuration with sensitive data
backend.config

# Local state files (if using local backend)
terraform.tfstate
terraform.tfstate.backup

# Local plans (if saving plans to files)
*.tfplan

# Generated files during apply
.terraform.tfstate.lock.info